spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/contact
    username: postgres
    password: postgres
    hikari:
      schema: contacts_schema
logging.level.root: INFO
app:
  init-contacts: false
